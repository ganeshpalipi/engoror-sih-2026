# ==========================================================
# RootVerse backend dependencies - Phase 1
# Python 3.11 recommended (Windows 11 tested target)
# Install:  pip install -r requirements.txt
# ==========================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Data validation / settings ---
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Database ---
sqlalchemy==2.0.36

# --- Testing (FastAPI TestClient needs httpx) ---
httpx==0.27.2
pytest==8.3.4

# --- Future phases (added when the modules land) ---
# Phase 3: (no new deps - SQLAlchemy only)
# Phase 5: torch/torchaudio, librosa or soundfile           (ASR)
# Phase 6: TTS engine deps, soundfile                        (TTS)
# Phase 10: reportlab or fpdf2                               (worksheet PDF)

# ==========================================================
# AI: Hindi -> Santali machine translation (IndicTrans2)
# All open-source, local inference. No cloud API, no API key.
# ==========================================================

# torch: on Windows `pip install torch` installs the CPU build by default -
# exactly what our CPU-first design wants. No GPU needed.
torch==2.5.1
transformers==4.46.3
sentencepiece==0.2.0
protobuf==4.25.5
safetensors>=0.4.5

# IndicProcessor: text normalization + language tagging for IndicTrans2.
# Official open-source toolkit. Requires Git on Windows (already a
# prerequisite of this project). The MODEL WEIGHTS come from Hugging Face
# (one-time gated download; see .env.example for HF_TOKEN steps).
IndicTransToolkit @ git+https://github.com/VarunGumma/IndicTransToolkit.git

# ==========================================================
# AI: Offline Hindi ASR (Phase 3) - faster-whisper
# CTranslate2 port of OpenAI Whisper. Open weights, local CPU
# inference (INT8), no cloud API. Model downloads ONCE (public
# repo, no HF token needed) into backend/model_cache/asr.
# ==========================================================
faster-whisper==1.1.1
# faster-whisper 1.1.1 imports `requests` without declaring it.
# (transformers already brings it in; pinned here so ASR-only installs work.)
requests==2.32.3

# Multipart/form-data parsing for audio file uploads (POST /api/asr/*)
python-multipart==0.0.20
